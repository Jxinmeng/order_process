{atomic_units_doc}

## 当前数据行

{row_json}

## 需要执行的动作

{actions_list}

请使用上述最小执行单元组合生成Python代码，实现所有动作。

要求：
1. 输入是 `row` 变量（dict类型）
2. 输出也是 `row` 变量（已修改）
3. 只输出Python代码，不要其他内容
4. 代码要健壮，处理可能的None值
5. 所有交期计算的结果必须写入 `row["交货日期"]`；绝不能覆盖输入字段 `row["日期"]`
6. 不要创建未定义字段（例如 `交货日期计算`）
